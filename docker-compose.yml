version: '3.8'

services:
    web:
        image: fastapi_app
        container_name: fastapi_app
        build:
            context: .
            dockerfile: Dockerfile
        ports:
            - "8000:80"
        volumes:
            - .:/app
        restart: always
